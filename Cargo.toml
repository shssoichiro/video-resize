[package]
name = "video-resize"
version = "0.1.0"
edition = "2021"
license = "MIT"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
anyhow = "1.0.66"
nalgebra = { version = "0.31.4", default-features = false, features = ["std"] }
num-traits = "0.2.15"
v_frame = "0.3.1"

[features]
default = []
# Enables extra code that is only useful for development of this library
devel = []
